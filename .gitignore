# Entornos Python
.env
__pycache__/
*.pyc
.venv/
venv/

# Mosquitto — credenciales generadas localmente
deploy/mosquitto/config/passwd

# Frontend
frontend/node_modules/
frontend/dist/

# Particiones NVS de aprovisionamiento — contienen credenciales reales
# (wifi_pass, api_key) de una unidad física específica, nunca deben commitearse
/nvs.bin
/firmware/nvs.bin
