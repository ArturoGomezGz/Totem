# Base de datos
POSTGRES_DB=totem
POSTGRES_USER=totem
POSTGRES_PASSWORD=changeme

# MQTT
MQTT_CLIENT_ID=totem-server
MQTT_USERNAME=server
MQTT_PASSWORD=changeme

# JWT — cambiar por un string aleatorio largo en produccion
JWT_SECRET=changeme-use-a-long-random-secret-in-production

# Servidor — IP o dominio accesible por los ESP32
SERVER_URL=http://192.168.1.X:8000

# Telegram — obtener de @BotFather y /getUpdates
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
